{
	"name": "@fal/github-commit-stats",
	"version": "0.1.0",
	"description": "GitHubコミット行数をカレンダーに表示し、ダッシュボードに統計を表示するプラグイン",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "tsc && esbuild src/index.ts --bundle --format=iife --outfile=dist/index.js --platform=browser --target=es2020 --global-name=plugin"
	},
	"devDependencies": {
		"@fal/plugin-types": "workspace:*",
		"@types/node": "^20.0.0",
		"esbuild": "^0.19.0",
		"typescript": "^5.3.0"
	}
}
