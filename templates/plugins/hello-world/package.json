{
	"name": "{{PLUGIN_ID}}",
	"version": "{{PLUGIN_VERSION}}",
	"description": "{{PLUGIN_DESCRIPTION}}",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --platform=browser --target=es2020",
		"watch": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --platform=browser --target=es2020 --watch",
		"test": "vitest run",
		"test:watch": "vitest watch",
		"clean": "rm -rf dist"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.3",
		"esbuild": "^0.24.0",
		"typescript": "^5.9.3",
		"vitest": "^4.0.6"
	}
}
