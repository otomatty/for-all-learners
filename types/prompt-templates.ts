/**
 * Prompt Templates Types
 *
 * Type definitions for prompt templates API and components
 *
 * DEPENDENCY MAP:
 *
 * Parents (Files that import these types):
 *   └─ app/(protected)/settings/_components/prompt-templates/index.tsx
 */

export interface PromptRow {
	prompt_key: string;
	template: string;
}
